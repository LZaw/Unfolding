
#include "Colorpalette.hpp"

namespace Colorpalette{
  const QColor criticalColor(Qt::red);
  const QColor cutTreeColor(Qt::darkRed);
  const QColor debugFillColor(Qt::white);
  const QColor debugFrameColor(Qt::black);
  const QColor highlightColor(Qt::green);
  const QColor imageLineColor(Qt::black);
  const QColor indirectNeighborColor(Qt::darkMagenta);
  const QColor lowlightColor(Qt::cyan);
  const QColor modelColor(Qt::darkBlue);
  const QColor neighborColor(Qt::magenta);
  const QColor predictingColor(Qt::darkGray);
  const QColor rootPolygonColor(255, 165, 0);
  const QColor unfoldTreeColor(65, 105, 225);
  const QColor unfoldTreeRootColor(255, 165, 0);
  const QColor unfoldPolygonFillColor(Qt::white);
  const QColor unfoldPolygonFrameColor(Qt::black);
  const QColor widgetBackgroundColor(Qt::white);
  const QColor windowBackgroundColor(Qt::black);
}
